{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Hospital App (Mac)",
            "type": "cppdbg",
            "request": "launch",
            // tasks.json'da oluşturduğumuz çıktı dosyası
            "program": "${workspaceFolder}/hospital_app",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true, // Terminali ayrı pencerede açar
            "MIMode": "lldb", 
            // tasks.json'daki label ile AYNI olmalı
            "preLaunchTask": "Build Hospital Project"
        }
    ]
}